version: "3.8"
services:
    db:
        image: postgres
        container_name: postgres_db
        restart: always
        environment:
          POSTGRES_PASSWORD: postgres
        ports: 
            - 5432:5432
